{
  "buildCommand": "npx prisma generate && next build",
  "framework": "nextjs",
  "regions": ["cdg1"],
  "crons": [
    {
      "path": "/api/cron/review-requests",
      "schedule": "0 8 * * *"
    }
  ]
}
